package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Base Surfaces
val AppBg = Color(0xFF0D0F0D)
val AppSurface = Color(0xFF161A15)
val AppCard = Color(0xFF1B201A)
val AppLine = Color(0xFF232823)

// Text
val AppInk = Color(0xFFE8ECE6)
val AppDim = Color(0xFF7D8A78)

// Semantic Accents
val AppAcid = Color(0xFFB6F24A)
val AppAmber = Color(0xFFF2B441)
val AppRed = Color(0xFFEC5A4D)
val AppBlue = Color(0xFF5AB0EC)
val AppViolet = Color(0xFFA98BF0)

// Aliases for compatibility
val MapBackground = AppBg
val SurfaceColor = AppSurface
val CardColor = AppCard
val DividerColor = AppLine

val BorderDark = AppLine
val BorderLight = AppLine

val TextPrimary = AppInk
val SectionTitleColor = AppDim
val TextSecondary = AppDim
val TextMuted = AppDim

val AcidGreen = AppAcid
val AcidGreenDim = AppAcid.copy(alpha = 0.5f)
val Amber = AppAmber
val Red = AppRed
val Blue = AppBlue
val Purple = AppViolet
